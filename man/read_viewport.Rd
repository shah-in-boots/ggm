% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overview.R
\name{read_viewport}
\alias{read_viewport}
\title{Read the data needed to draw a window}
\usage{
read_viewport(
  cache,
  window,
  channels = NULL,
  width_px = NULL,
  resolution = c("auto", "raw", "overview")
)
}
\arguments{
\item{cache}{A \code{StudyCache}.}

\item{window}{A list holding \code{begin} and \code{end} sample indices delimiting a
half-open range, as \code{\link[=normalize_selection]{normalize_selection()}} returns.}

\item{channels}{Channel labels or indices. Defaults to all channels.}

\item{width_px}{Width of the plot in pixels. Required unless
\code{resolution = "raw"}.}

\item{resolution}{\code{"auto"} chooses by window and width, \code{"raw"} always
reads the signal, and \code{"overview"} always reads a cache level, the
finest one when the window is small.}
}
\value{
A list with \code{data}, \code{resolution}, and \code{level}. For raw reads
\code{data} is the signal table (\code{sample} plus one column per channel),
\code{resolution} is \code{"raw"} and \code{level} is \code{0}. For overview reads \code{data} is
a named list with one \code{list(sample, value)} per channel, the minima and
maxima of each bucket in sample order with a flat bucket's pair collapsed
to one point; \code{resolution} is the bucket size in samples and \code{level} its
position in the pyramid. Extrema fall at different samples per channel,
which is why the two shapes differ.
}
\description{
Routes a sample window to the raw signal or to an overview level. Raw is
used when the window already holds few enough samples to draw; otherwise
the finest overview level that keeps the plotted points near the design's
four per pixel per channel. An overview is never used for measurement or
snapping; it is for drawing.
}
\seealso{
\code{\link[=build_overview]{build_overview()}}, \code{\link[=read_study_signal]{read_study_signal()}}
}
