% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{convert}
\alias{convert}
\alias{time_to_sample}
\alias{sample_to_time}
\title{Convert between sample index and time}
\usage{
time_to_sample(time, frequency)

sample_to_time(sample, frequency)
}
\arguments{
\item{time}{Numeric vector of times, in \strong{seconds}.}

\item{frequency}{Sampling frequency in Hz (samples per second). A single
positive number, typically \code{study$frequency}.}

\item{sample}{Numeric (integer) vector of \strong{sample indices}.}
}
\value{
\itemize{
\item \code{time_to_sample()}: an integer vector of sample indices (rounded).
\item \code{sample_to_time()}: a double vector of times in seconds.
}
}
\description{
The sample index (an integer count of samples from the start of the record)
is the canonical key used for storage and joins; time in seconds is the
display unit. Conversion is exact given the recording's sampling frequency:
\code{time = sample / frequency} and \code{sample = round(time * frequency)}.
}
\details{
\code{time_to_sample()} rounds to the nearest sample because a time need not land
exactly on a sample boundary; \code{sample_to_time()} is exact. Round-tripping a
valid sample index (\code{sample_to_time()} then \code{time_to_sample()}) returns the
original index.
}
\examples{
time_to_sample(c(0, 1, 1.5), frequency = 1000)
sample_to_time(c(0L, 1000L, 1500L), frequency = 1000)

}
