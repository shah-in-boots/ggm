% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-plot.R
\name{gram_plot}
\alias{gram_plot}
\title{Create an electrogram widget}
\usage{
gram_plot(
  panels,
  window = NULL,
  extent = NULL,
  backend = c("uplot"),
  scale = list(kind = "index", rate = 1),
  panel_height = 120,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{panels}{List of panels, one per channel. Each is a list holding
numeric \code{x} and \code{y} of equal length, and optionally a \code{label} and a
\code{color}. Lengths may differ between panels.}

\item{window}{Loaded range as \code{list(min =, max =)} in x units. Panels are
drawn against this rather than against their own extents, so overview
panels whose extrema fall on different samples still line up. Defaults to
the union of the panel x extents.}

\item{extent}{Record range as \code{list(min =, max =)} in x units. Panning is
clamped to this, so a reader may pan beyond what is loaded and have the
controller fill it in. Defaults to \code{window}.}

\item{backend}{Renderer to draw with. Its spec is built here in R and its
library is attached to the widget, both resolved by \code{gm_backend()}.}

\item{scale}{X-scale description. \code{kind} may be \code{"index"} for sample
numbers, \code{"elapsed"} for elapsed seconds, or \code{"timestamp"} for Unix
timestamps.}

\item{panel_height}{Height of each channel panel in CSS pixels.}

\item{width, height}{Optional widget dimensions.}

\item{elementId}{Optional HTML element id.}
}
\value{
An \code{htmlwidget}.
}
\description{
\code{gram_plot()} is the low-level plotting primitive. It renders one panel per
signal channel so each has an independent y-scale, while synchronising the
x-range and cursor across panels. Each panel carries its own \code{x} and \code{y},
which is what lets an overview tier draw its per-channel extrema; panels may
differ in length. The backend named in \code{backend} turns the panels into what
its library wants, in R, and only that backend's assets travel with the
widget. Use \code{\link[=view_uplot]{view_uplot()}} to read and display a window from a \code{StudyCache}
directly.
}
\seealso{
\code{\link[=view_uplot]{view_uplot()}}, \code{\link[=read_viewport]{read_viewport()}}
}
