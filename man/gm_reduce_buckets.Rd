% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overview.R
\name{gm_reduce_buckets}
\alias{gm_reduce_buckets}
\title{Reduce a signal to per-bucket extrema}
\usage{
gm_reduce_buckets(values, samples, bucket)
}
\arguments{
\item{values}{Numeric vector.}

\item{samples}{Integer vector, the sample index of each value.}

\item{bucket}{Number of consecutive values per bucket.}
}
\value{
A data frame with one row per bucket and columns \code{min}, \code{min_at},
\code{max}, \code{max_at}. A bucket containing \code{NA} reports \code{NA}, so a gap in the
signal stays a gap in the overview.
}
\description{
Splits \code{values} into consecutive runs of \code{bucket} and returns the minimum
and maximum of each run together with the sample index at which each
occurred. It serves the first overview level, where \code{values} is raw signal,
and every merge above it, where \code{values} is the level below's minima or
maxima and \code{samples} their positions.
}
\keyword{internal}
