% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harness.R
\name{gram_harness}
\alias{gram_harness}
\title{Launch the development harness}
\usage{
gram_harness(
  cache,
  window = NULL,
  channels = NULL,
  backend = c("uplot", "plotly"),
  script = harnessScript,
  ...
)
}
\arguments{
\item{cache}{A \code{StudyCache} from \code{\link[=study_cache]{study_cache()}}.}

\item{window}{Sample range the viewer opens on, as \code{list(begin =, end =)}.
Defaults to the whole record.}

\item{channels}{Channels loaded into the viewer panel. Defaults to every
channel in the header.}

\item{backend}{Renderer for the viewer panel; see \code{\link[=gram_plot]{gram_plot()}}.}

\item{script}{Initial contents of the scripting panel.}

\item{...}{Passed to \code{\link[shiny:shinyApp]{shiny::shinyApp()}}.}
}
\value{
A Shiny app object.
}
\description{
Opens a page with three panels: the signal viewer, a scripting panel for the
tracing grammar, and the compiled tracing. Scripts are evaluated by
\code{\link[=eval_tracing]{eval_tracing()}}, not by R.
}
\details{
The viewer opens on the whole record. At a coarse overview tier that is the
study navigator, so a reader starts by seeing the study rather than an
opening slice of it. Dragging across a panel zooms; the wheel pans; both
report the range wanted and are answered with whichever tier fits it. The
harness is the controller: it knows the backend by name only, and every
change it makes -- a new window, a new tier, a different set of channels --
is one push of a fresh spec into the live widget.
}
\seealso{
Other controller:
\code{\link{gram_channels}},
\code{\link[=normalize_selection]{normalize_selection()}}
}
\concept{controller}
