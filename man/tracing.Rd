% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tracing.R
\name{tracing}
\alias{tracing}
\title{Open a signal window as a tracing}
\usage{
tracing(
  cache,
  begin = NULL,
  end = NULL,
  interval = NULL,
  channels = NULL,
  units = c("physical", "digital"),
  max_points = 2000
)
}
\arguments{
\item{cache}{A \code{StudyCache} from \code{\link[=study_cache]{study_cache()}}.}

\item{begin, end}{Times delimiting a half-open range. These follow
\code{\link[EGM:validate_time_parameters]{EGM::validate_time_parameters()}}: time-only character values are elapsed
from the record start, dated character values and \code{POSIXt} objects are
absolute, and \code{difftime} values are elapsed durations. Numeric values are
not accepted.}

\item{interval}{A duration after \code{begin} that takes precedence over \code{end}.
Numeric values are seconds; compact durations such as \code{"100 ms"} are also
accepted.}

\item{channels}{Channel labels or indices. Defaults to all channels.}

\item{units}{Units passed to \code{\link[EGM:read_signal]{EGM::read_signal()}}.}

\item{max_points}{Largest number of points kept per channel. Longer
windows are decimated by a fixed stride, which can drop a narrow
deflection; keep presentation windows short rather than raising this.}
}
\value{
A \code{Tracing}.
}
\description{
Reads one window from the canonical signal and returns a \code{Tracing} with
an empty script. Verbs append operations to it.
}
\seealso{
Other tracing:
\code{\link[=at]{at()}},
\code{\link[=gram_tracing]{gram_tracing()}},
\code{\link[=tracing_spec]{tracing_spec()}}
}
\concept{tracing}
