% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-plotly.R
\name{view_plotly}
\alias{view_plotly}
\title{View a study window with plotly}
\usage{
view_plotly(
  cache,
  window = NULL,
  channels = NULL,
  width_px = 1200,
  resolution = c("auto", "raw", "overview"),
  width = NULL,
  height = 500
)
}
\arguments{
\item{cache}{A \code{StudyCache} created by \code{\link[=study_cache]{study_cache()}}.}

\item{window}{Sample range as \code{list(begin =, end =)}, half-open. Defaults to
the whole record.}

\item{channels}{Channel labels or indices. Defaults to all channels.}

\item{width_px}{Width of the plot in pixels. Required unless
\code{resolution = "raw"}.}

\item{resolution}{\code{"auto"} chooses by window and width, \code{"raw"} always
reads the signal, and \code{"overview"} always reads a cache level, the
finest one when the window is small.}

\item{width, height}{Optional widget dimensions.}
}
\value{
An \code{htmlwidget}.
}
\description{
Reads one window from a \code{StudyCache} and returns the plotly widget: cache
and window in, widget out, no controller. It is the one-call check that
this backend draws a window, and it lives beside the spec it exercises.
Needs the \code{plotly} package, whose plotly.js bundle is borrowed rather than
vendored.
}
\details{
The window is given in samples, the same currency \code{\link[=read_viewport]{read_viewport()}} and
\code{\link[=normalize_selection]{normalize_selection()}} speak. The whole record is a valid window and the
default.
}
\seealso{
\code{\link[=view_uplot]{view_uplot()}}, \code{\link[=gram_plot]{gram_plot()}}

Other backend viewers:
\code{\link[=view_uplot]{view_uplot()}}
}
\concept{backend viewers}
\keyword{internal}
