% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window.R
\name{get_window}
\alias{get_window}
\title{Read a window of signal}
\usage{
get_window(
  study,
  channels = NULL,
  begin = 0,
  end = NA,
  px_width = NULL,
  units = c("physical", "digital")
)
}
\arguments{
\item{study}{A \code{ggm_study} from \code{\link[=open_study]{open_study()}}.}

\item{channels}{Character vector of channel labels to read. \code{NULL} (default)
reads every channel in the study.}

\item{begin, end}{Window bounds in \strong{seconds}. \code{end = NA} (default) reads to
the end of the record.}

\item{px_width}{Width of the target viewport in pixels. Optional; when given,
it is used to compute points-per-pixel and (in a future milestone) choose a
downsampled tier. It does not change the data in this spine release.}

\item{units}{\code{"physical"} (mV, default — what you plot) or \code{"digital"} (raw
ADC integers). Passed through to \code{\link[EGM:read_signal]{EGM::read_signal()}}.}
}
\value{
A \code{ggm_window}: an \code{EGM} \code{signal_table} (a \code{data.table}) with a
\code{sample} column, a \code{time} column (seconds), and one column per requested
channel. Carries attributes \code{tier} (which source served it — \code{"raw"} for a
direct \code{.dat} read), \code{ppp} (points-per-pixel, or \code{NA}), and \code{frequency}.
}
\description{
The adaptive windowed read that feeds the renderers. Give it a time window
(seconds) and the channels you want; it returns those channels over that
window with both \code{sample} and \code{time} columns.
}
\examples{
\dontrun{
study <- open_study("bard-egm", system.file("extdata", package = "ggm"))
w <- get_window(study, channels = c("HIS D", "RV 1-2"), begin = 1, end = 2)
attr(w, "tier")
}
}
