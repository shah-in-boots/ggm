% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study.R
\name{open_study}
\alias{open_study}
\alias{is_study}
\title{Open a study}
\usage{
open_study(record, record_dir = ".")

is_study(x)
}
\arguments{
\item{record}{Record name, without file extension (e.g. \code{"bard-egm"}). The
\code{.hea}/\code{.dat} files are expected to share this stem.}

\item{record_dir}{Directory containing the record's files. Defaults to the
current directory.}

\item{x}{A \code{ggm_study} (for \code{is_study()}) or any object.}
}
\value{
A \code{ggm_study}: a list with the record location, the parsed \code{EGM}
\code{header_table}, and convenience fields pulled from the header
(\code{frequency} in Hz, \code{n_samples}, \code{channels}, \code{start_time}, and \code{duration}
in seconds).

\code{is_study()} returns a length-one logical.
}
\description{
Bind a WFDB record to a lightweight \code{ggm_study} object. Only the header is
read on open (cheap); signal samples are read on demand by \code{\link[=get_window]{get_window()}}.
}
\details{
This wraps \code{\link[EGM:read_header]{EGM::read_header()}} and pulls the recording metadata from its
\code{record_line} attribute. The signal store stays canonical in \code{EGM}; \code{ggm}
only remembers how to reach it.
}
\examples{
\dontrun{
study <- open_study("bard-egm", system.file("extdata", package = "ggm"))
study
}
}
