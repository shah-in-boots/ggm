% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{gm_read_manifest}
\alias{gm_read_manifest}
\title{Read the cache section of a study manifest}
\usage{
gm_read_manifest(cache)
}
\arguments{
\item{cache}{A \code{StudyCache}.}
}
\value{
The \code{cache} section as a list, or \code{NULL} when the manifest or the
section is absent, the section was written by another format version,
its fingerprint is not the open record's (the record changed since the
build), or the table it names is missing. \code{NULL} is the answer to "may I
use this overview", and every caller treats it as "not built" rather than
serving a stale reduction as if it were current.
}
\description{
\verb{<stem>.gram.json} is shared between the parts of gram that write beside a
record; each owns one section. This reads the \code{cache} section and decides
whether the overview it describes may be used for the open record.
}
\keyword{internal}
