% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-data.R
\name{cache_example_data}
\alias{cache_example_data}
\title{Download an example study to the local cache}
\usage{
cache_example_data(
  dataset = "ort",
  dir = tools::R_user_dir("ggm", "cache"),
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dataset}{Example record name. Currently only \code{"ort"} (the default): a
27-channel intracardiac study, 977 Hz, ~2.7 hours (~516 MB).}

\item{dir}{Directory to cache into. Defaults to the package's per-user cache
(\code{tools::R_user_dir("ggm", "cache")}), the CRAN-sanctioned location for
downloaded data. Pass e.g. \code{"data-raw"} to download elsewhere.}

\item{force}{Re-download even if the files are already present (useful if a
cached copy is suspected truncated).}

\item{quiet}{Passed to \code{\link[utils:download.file]{utils::download.file()}}; \code{FALSE} (default) shows a
progress bar, reassuring for a multi-hundred-MB transfer.}
}
\value{
The path to \code{dir} (invisibly when a download happened, so a fresh
pull does not dump a path mid-pipeline). The record's \code{.hea}/\code{.dat} are
guaranteed present on return, so
\code{open_study(dataset, cache_example_data(dataset))} always works.
}
\description{
Fetch a \code{ggm} example WFDB record to a local directory, downloading it once
and reusing the cached copy thereafter. A WFDB record is a pair of files that
together form one dataset — a \code{.hea} text header and a \code{.dat} binary signal —
and both are fetched. The returned directory drops straight into
\code{\link[=open_study]{open_study()}}.
}
\details{
Each file downloads to a \code{.part} sidecar and is renamed into place only on
success, so an interrupted transfer never leaves a truncated file that later
looks "cached". The download timeout is raised for the call (the 60 s default
is far too short for these files) and restored on exit.
}
\examples{
\dontrun{
# Download (once) and open the large ORT study:
study <- open_study("ort", cache_example_data("ort"))
study
}
}
\seealso{
\code{\link[=open_study]{open_study()}} to open the downloaded record.
}
