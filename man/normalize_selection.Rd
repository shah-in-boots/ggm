% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend.R
\name{normalize_selection}
\alias{normalize_selection}
\title{Canonicalise a viewer selection to a sample range}
\usage{
normalize_selection(cache, selection)
}
\arguments{
\item{cache}{A \code{StudyCache}.}

\item{selection}{The widget's selection input: a list holding \code{xmin} and
\code{xmax} in elapsed seconds. \code{NULL} before anything has been selected.}
}
\value{
A list with \code{begin} and \code{end} sample indices delimiting a half-open
range, clamped to the record. \code{NULL} both when nothing is selected yet and
when the selection collapses to no samples, so a stray click leaves the
caller's current window standing rather than blanking it.
}
\description{
A \code{\link[=gram_plot]{gram_plot()}} widget reports a completed drag as an
\verb{<outputId>_selection} Shiny input carrying \code{xmin} and \code{xmax} in the
widget's display units -- elapsed seconds, for the x-scale \code{\link[=view_uplot]{view_uplot()}}
builds. Sample index is the canonical key, so the conversion happens here
once rather than in every caller.
}
\seealso{
Other backend viewers:
\code{\link[=view_uplot]{view_uplot()}}
}
\concept{backend viewers}
