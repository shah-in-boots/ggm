% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{read_study_viewport}
\alias{read_study_viewport}
\title{Read the appropriate data for a viewport}
\usage{
read_study_viewport(
  cache,
  begin = NULL,
  end = NULL,
  interval = NULL,
  channels = NULL,
  pixel_width,
  resolution = c("auto", "raw", "overview"),
  points_per_pixel = 4,
  units = c("physical", "digital")
)
}
\arguments{
\item{cache}{A \code{StudyCache}.}

\item{begin, end}{Times delimiting a half-open range. These follow
\code{\link[EGM:validate_time_parameters]{EGM::validate_time_parameters()}}: time-only character values are elapsed
from the record start, dated character values and \code{POSIXt} objects are
absolute, and \code{difftime} values are elapsed durations. Numeric values are
not accepted.}

\item{interval}{A duration after \code{begin} that takes precedence over \code{end}.
Numeric values are seconds; compact durations such as \code{"100 ms"} are also
accepted.}

\item{channels}{Channel labels or indices.}

\item{pixel_width}{Plot width in device pixels.}

\item{resolution}{\code{"auto"}, \code{"raw"}, or \code{"overview"}.}

\item{points_per_pixel}{Target maximum rendered points per pixel.}

\item{units}{Raw read units.}
}
\value{
A list with \code{data}, \code{resolution}, and \code{level}.
}
\description{
Read the appropriate data for a viewport
}
