% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-viewer.R
\name{view_uplot}
\alias{view_uplot}
\title{View a study window with the uPlot backend}
\usage{
view_uplot(
  cache,
  begin = NULL,
  end = NULL,
  interval = NULL,
  channels = NULL,
  units = c("physical", "digital"),
  width = NULL,
  height = 500
)
}
\arguments{
\item{cache}{A \code{StudyCache} created by \code{\link[=study_cache]{study_cache()}}.}

\item{begin, end}{Times delimiting a half-open range. These follow
\code{\link[EGM:validate_time_parameters]{EGM::validate_time_parameters()}}: time-only character values are elapsed
from the record start, dated character values and \code{POSIXt} objects are
absolute, and \code{difftime} values are elapsed durations. Numeric values are
not accepted.}

\item{interval}{A duration after \code{begin} that takes precedence over \code{end}.
Numeric values are seconds; compact durations such as \code{"100 ms"} are also
accepted.}

\item{channels}{Channel labels or indices. Defaults to all channels.}

\item{units}{Signal units passed to \code{\link[=read_study_signal]{read_study_signal()}}.}

\item{width, height}{Optional widget dimensions.}
}
\value{
An \code{htmlwidget}.
}
\description{
\code{view_uplot()} is a backend-specific development viewer. It reads a fixed
interval from the canonical WFDB signal, adapts the result to uPlot's aligned
column format, and returns the standalone uPlot widget. It is useful for
exercising and testing the uPlot backend without a Shiny controller.
}
\details{
The widget contains only the requested \code{begin} to \code{end} or \code{interval} range.
When neither \code{end} nor \code{interval} is supplied, the viewer reads ten seconds
from \code{begin}. Drag horizontally to zoom into that loaded range, scroll
horizontally (or use Shift+wheel) to pan, and double-click to reset the
range. A future interactive study controller can manage dynamic viewport
reads while reusing the same uPlot backend.
}
\concept{backend viewers}
